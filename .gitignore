.terraform/
terraform.tfstate
terraform.tfstate.backup
*.tf.bak
*.txt
# Terraform build artifacts
tfplan
lambda/*.zip
lambda/tutor.html
lambda/proxy.py
# Actual tfvars files may contain customer PII (emails, lab names) — never commit
# Only the .example template is tracked
*.tfvars
!*.tfvars.example
# Checkpoint documents contain instance IDs, IP addresses, and internal infra details
# Generate fresh from the project at any time; do not store in version control
*Checkpoint*.docx
# All generated documents (PoC guide, deployment guide, customer guide PDF) —
# these are outputs, not source. Regenerate from the project Markdown/scripts.
*.docx
*.pdf

