# Rate limiting for /chat endpoint
# Must be in the http context (included from /etc/nginx/conf.d/).
# 10 requests per minute per IP; burst of 5 allowed before 429 is returned.
limit_req_zone $binary_remote_addr zone=chat_limit:10m rate=10r/m;
